package com.game;

import java.util.ResourceBundle;

public class Testing {

//    ResourceBundle bundle = ResourceBundle.getBundle(BUNDLE_NAME);
// ConsoleReferee referee = new ConsoleRefererr(strategy, presentation, scanner, bundle, taxer, maxMove, state);
    // while (keepPlaying(scanner..)
    // is the input data in the correct form? - Controller
    // taking data from the user = View type Class
    // doing something with the data and saying that choice is invalid = model
    //what cards remain on the table
    //what cards are in the deck
    // what are the wagers? pot, bank etc..
    // presenting the information to the user in the current state. Other play and your wager
    // add current wager to the menu
}
